#download the android SDK automatically
sudo apt-get --force-yes --yes install expect #install expect to auto say yes to android license agreements.
./get-sdk.sh #check if the sdk is installed, and if not, install it
